#include <stdio.h>
int main(){
    int Nazmi;
    scanf("%d", &Nazmi);
    if (Nazmi >= 1)
    {
        printf("positif");
    } else if (Nazmi < 0)
    {
        printf("negatif");
    } else
    {
        printf("nol");
    }
    
}